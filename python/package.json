{
  "scripts": {
    "tensorboard": "python pb_to_tensorboard.py && tensorboard --logdir=run1:./log",
    "start": "jupyter notebook",
    "tfjs": "tensorflowjs_converter --input_format=tf_frozen_model --output_node_names=loss --saved_model_tags=serve ./exportedModel/model.pb  ../js/assets"
  }
}